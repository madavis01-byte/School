# Lumen Sandbox Academy

Start the self-hosted website and WebRTC live rooms with:

```bash
npm start
```

Then open [http://localhost:4173](http://localhost:4173). Live session rooms use the local WebRTC signaling server; open the same room in another browser window to test participants.
# School